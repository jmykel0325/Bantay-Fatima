package com.bantayfatima.app.ui.theme

import androidx.compose.ui.graphics.Color

// ---------------------------------------------------------------------------
// Bantay Fatima brand palette, shared with the Laravel web application.
// ---------------------------------------------------------------------------
val BantayGreen = Color(0xFF0F8877)
val BantayNavy = Color(0xFF0B2D4D)
val BantayBackground = Color(0xFFF4F7F9)
val BantayWhite = Color(0xFFFFFFFF)
val BantayBorder = Color(0xFFDCE4EA)
val BantayMuted = Color(0xFF6B8196)

// Derived brand tones -------------------------------------------------------
val BantayGreenDark = Color(0xFF0B6D5F)
val BantayGreenSoft = Color(0xFFE3F3F0)
val BantayNavyDeep = Color(0xFF07203A)
val BantayNavySoft = Color(0xFFE7EEF5)
val BantayGreenLight = Color(0xFF5FCEBB)
val BantayNavyLight = Color(0xFF9CC0DD)

// ---------------------------------------------------------------------------
// Report status palette. Every status is expressed by colour *and* text, never
// colour alone, so the meaning survives for colour-blind residents.
// ---------------------------------------------------------------------------
val StatusPending = Color(0xFFF5A623)
val StatusInProgress = Color(0xFF2F80ED)
val StatusResolved = Color(0xFF15966A)
val StatusUrgent = Color(0xFFD64545)

// Readable text tones for use on the soft containers below.
val StatusPendingText = Color(0xFF8A5A00)
val StatusInProgressText = Color(0xFF15508F)
val StatusResolvedText = Color(0xFF0B5D3B)
val StatusUrgentText = Color(0xFF8F2020)

val StatusPendingSoft = Color(0xFFFDF1DC)
val StatusInProgressSoft = Color(0xFFE4EFFC)
val StatusResolvedSoft = Color(0xFFDFF2EA)
val StatusUrgentSoft = Color(0xFFFBE5E5)
val StatusNeutralSoft = Color(0xFFEDF2F6)

// ---------------------------------------------------------------------------
// Dark theme surfaces
// ---------------------------------------------------------------------------
val BantayDarkBackground = Color(0xFF08192A)
val BantayDarkSurface = Color(0xFF0F2740)
val BantayDarkSurfaceHigh = Color(0xFF163350)
val BantayDarkBorder = Color(0xFF244665)
val BantayDarkMuted = Color(0xFFA7BED2)
