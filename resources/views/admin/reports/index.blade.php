<x-admin.layout title="Community Reports" :breadcrumbs="['Administration','Community Reports']">
<div class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-navy/5">
<form class="grid gap-3 md:grid-cols-3 xl:grid-cols-6" method="GET"><input class="rounded-xl border border-navy/15 px-3 py-2.5 xl:col-span-2" name="q" value="{{ request('q') }}" placeholder="Search reports or residents"><select name="status" class="rounded-xl border border-navy/15 px-3 py-2.5"><option value="">All statuses</option>@foreach(['pending','in_progress','resolved'] as $v)<option value="{{ $v }}" @selected(request('status')===$v)>{{ str($v)->replace('_',' ')->title() }}</option>@endforeach</select><select name="priority" class="rounded-xl border border-navy/15 px-3 py-2.5"><option value="">All priorities</option>@foreach(['low','normal','high','urgent'] as $v)<option value="{{ $v }}" @selected(request('priority')===$v)>{{ ucfirst($v) }}</option>@endforeach</select><select name="category_id" class="rounded-xl border border-navy/15 px-3 py-2.5"><option value="">All categories</option>@foreach($categories as $v)<option value="{{ $v->id }}" @selected(request('category_id')==$v->id)>{{ $v->name }}</option>@endforeach</select><select name="purok_id" class="rounded-xl border border-navy/15 px-3 py-2.5"><option value="">All puroks</option>@foreach($puroks as $v)<option value="{{ $v->id }}" @selected(request('purok_id')==$v->id)>Purok {{ $v->name }}</option>@endforeach</select><input type="date" name="date_from" value="{{ request('date_from') }}" class="rounded-xl border border-navy/15 px-3 py-2.5"><input type="date" name="date_to" value="{{ request('date_to') }}" class="rounded-xl border border-navy/15 px-3 py-2.5"><button class="rounded-xl bg-teal px-4 py-2.5 font-semibold text-white">Apply filters</button><a href="{{ route('admin.reports.index') }}" class="rounded-xl border border-navy/15 px-4 py-2.5 text-center font-semibold">Clear</a><a href="{{ route('admin.reports.export',request()->query()) }}" class="rounded-xl bg-navy px-4 py-2.5 text-center font-semibold text-white">Export CSV</a></form>
</div>
<div class="mt-5 overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-navy/5"><div class="overflow-x-auto"><table class="w-full min-w-[1050px] text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-navy/50"><tr>@foreach(['Reference','Title','Reporter','Category','Purok','Submitted','Priority','Status','Staff','Actions'] as $h)<th class="px-4 py-3">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y">@forelse($reports as $report)<tr class="hover:bg-slate-50/60"><td class="px-4 py-4 font-semibold">{{ $report->reference_number }}</td><td class="max-w-48 truncate px-4">{{ $report->title }}</td><td class="px-4">{{ $report->resident->first_name }} {{ $report->resident->last_name }}</td><td class="px-4">{{ $report->category->name }}</td><td class="px-4">{{ $report->purok->name }}</td><td class="px-4">{{ $report->created_at->format('M d, Y') }}</td><td class="px-4"><x-admin.badge :value="$report->priority" /></td><td class="px-4"><x-admin.badge :value="$report->status" /></td><td class="px-4">{{ $report->assignedStaff?->first_name ?? 'Unassigned' }}</td><td class="px-4"><a class="font-semibold text-teal" href="{{ route('admin.reports.show',$report) }}">View</a></td></tr>@empty<tr><td colspan="10"><x-admin.empty-state /></td></tr>@endforelse</tbody></table></div><div class="border-t p-4">{{ $reports->links() }}</div></div>
</x-admin.layout>
